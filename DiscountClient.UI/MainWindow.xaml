<Window x:Class="DiscountClient.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Title="Discount Code Client" Height="600" Width="450">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Discount Code System" Style="{StaticResource MaterialDesignHeadline4TextBlock}" Margin="0,0,0,24"/>

        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Generate Codes" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,8"/>
                <TextBox x:Name="CountTextBox" materialDesign:HintAssist.Hint="Number of Codes (max 2000)" Margin="0,8"/>
                <TextBox x:Name="LengthTextBox" materialDesign:HintAssist.Hint="Code Length (7 or 8)" Margin="0,8"/>
                <Button x:Name="GenerateButton" Content="Generate" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,16,0,0" Click="GenerateButton_Click"/>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Padding="16">
            <StackPanel>
                <TextBlock Text="Use Code" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,8"/>
                <TextBox x:Name="CodeToUseTextBox" materialDesign:HintAssist.Hint="Enter Discount Code" Margin="0,8"/>
                <Button x:Name="UseCodeButton" Content="Use Code" Style="{StaticResource MaterialDesignRaisedLightButton}" Margin="0,16,0,0" Click="UseCodeButton_Click"/>
            </StackPanel>
        </materialDesign:Card>

        <TextBox Grid.Row="3" x:Name="ResponseTextBox" IsReadOnly="True" Margin="0,24,0,0"
                 materialDesign:HintAssist.Hint="Server Responses"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Height="Auto" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>